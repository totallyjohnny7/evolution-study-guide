/* Stub: makeover.js is intentionally a no-op for the evolution site.
   The original biol3020 makeover.js relabeled chapter nav, rewrote the hero,
   and forced the site into a Flash-only mode. None of that applies here.
   Evolution content is served directly from window.MANUAL_LECTURE_GUIDES,
   window.STIM_BANK, and window.FLASHCARD_DECKS. */
console.log('[makeover] stubbed - no overrides for evolution site');
